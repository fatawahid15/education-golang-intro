package main

import "fmt"

type EmployeeApple struct {
	FirstName string
	LastName  string
	Age       int
}

type EmployeeGoogle struct {
	FirstName string
	LastName  string
	Age       int
}

func main() {
	// Pascal case
	// Ex:: CalculateArea, UserInfo
	// Structs, Interfaces, enums

	// snake_case
	// Ex: user_id, first_name, what_ever

	//UPPERCASE
	// Exclusively for naming constants, so it easily recognizeable, constant are immutable

	//mixedCase
	// Ex: javaScript, htmlDocument, isValid <= kinda have 0 difference with camelCase

	// ^ minimize the use of abbreviation, unless it widely understandable

	const MAXRETRIES = 5 // <= depends on the project setting or sumtin like that

	var employeeID = 1001
	fmt.Println("EmployeeID: ", employeeID)
}
